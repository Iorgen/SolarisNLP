# import logging
# from pathlib import Path
# from dotenv import load_dotenv
#
# logger = logging.getLogger(__name__)
#
#
# # calculate relative to file location
# root_project_dir = Path(__file__).parent.parent.parent
#
# env_file_path = (root_project_dir / ".env").resolve()
# load_dotenv(env_file_path, override=False, verbose=True)
# logger.info(f"Loaded .env from {env_file_path}")
